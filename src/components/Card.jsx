import React from 'react';import styled from "styled-components";const Wrapper = styled.article`  box-shadow: var(--shadow);  border-radius: var(--radii);  background-color: var(--colors-ui-base);  overflow: hidden;  cursor: pointer;`;const CardImg = styled.img`  display: block;  width: 100%;  height: 150px;  box-shadow: var(--shadow);  object-fit: cover;  object-position: center;`;const CardBody = styled.div`  padding: 1rem 1.5rem 0 `;const CardTitle = styled.h3`  margin: 0;  padding-bottom: 0.5rem;  font-size: var(--fs-md);  font-weight: var(--fw-bold);`;const CardList = styled.ul`  margin: 0;  padding: 1rem 0;`;const CardListItem = styled.li`  list-style: none;  font-size: var(--fs-sm);  line-height: 1.5;  font-weight: var(--fw-light);  & > b {    font-weight: var(--fw-bold);  }`;const Card = ({img, name, language, info = [], onClick}) => {    return (        <Wrapper onClick={onClick}>            <CardImg src={img}/>            <CardBody>                <CardTitle>{name.common}</CardTitle>                <span><b>Official name:</b> {name.official}</span>                <CardList>                    {Object.keys(language).map(langName => (                        <CardListItem key={langName}>                            <b>Language ({langName}):</b> {language[langName]}                        </CardListItem>                    ))}                    {info.map(el => (                            (<CardListItem key={el.title}>                                <b>{el.title}:</b> {el.description}                            </CardListItem>)                    ))}                </CardList>            </CardBody>        </Wrapper>    );};export default Card;