const BASE_URL = 'https://restcountries.com/v3.1/';export const findByCountry = (name) => BASE_URL + 'name/' + name;export const ALL_COUNTRIES = BASE_URL +'/all?fields=name,capital,flags,population,languages,region';export const filterByCode = (codes) => BASE_URL + 'alpha?codes=' + codes.join(',')